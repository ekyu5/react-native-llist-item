import React from "react";
import { View } from "react-native";

export default class CarrotTabEmpty extends React.Component {
    render() {
        return (
            <View></View>
        );
    }
}